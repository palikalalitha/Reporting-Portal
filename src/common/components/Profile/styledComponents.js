import styled from "@emotion/styled"
import tw  from "tailwind.macro"

const ProfileContainer=styled.div `${tw`flex`}`
const ViewProfileData=styled.div `${tw`flex flex-col justify-center`}`
const WrapInPutElements=styled.div `${tw``}`
export {ProfileContainer,ViewProfileData,WrapInPutElements}
